@model List<PedidoSituacaoStatus>
@{
    ViewData["Title"] = "Default";
}

@*@foreach (var pedidosSituacao in Model.PedidoSituacoes)
    {
        <span>
            @pedidosSituacao.Situacao
            @pedidosSituacao.Data
        </span>
    }*@

@if (Model != null)
{
    <ul class="timeline" id="timeline">
        @foreach (var pedidosStatus in Model)
        {
            <li class="li @((pedidosStatus.Concluido) ? pedidosStatus.Cor : "")">
                <div class="timestamp">
                    <span class="date">@((pedidosStatus.Data !=null)?pedidosStatus.Data.ToString() : "-")</span>
                </div>
                <div class="status">
                    <h4> @pedidosStatus.Situacao </h4>
                </div>
            </li>

        }

    </ul>
}


